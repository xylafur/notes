- Architecture
    - the manner with which the processor, random access memory
      (RAM), read only memory (ROM), and input/output (I/O) ports are combined
      to create the microcontroller.

