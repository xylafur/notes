

int main(){
    long apple;
    int p = 2;

    //Trying to determine the type of apple..
    apple = sizeof (int) * p;

    // It is indeed a long, this means that sizeof encapselates the thing closes, or
    // possibly just the thing that is next to it and in parenthesis
}
